import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hello_ionic_components',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
